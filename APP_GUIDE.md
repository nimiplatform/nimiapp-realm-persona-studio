# Realm Persona Studio

创建和管理自己的 Realm Persona，维护角色资料与创作草稿。

## 使用方式

1. 打开自己的 Persona 列表，选择已有角色或创建新角色。
2. 编辑角色资料，整理视觉身份与内容创作草稿。
3. 保存前检查要提交的内容；保存结果以 Realm 返回的实际结果为准。

需要平台支持的动作会显示当前可用状态，角色的本地草稿与已保存的 Realm 资料分别保留。

## 运行条件

在 Nimi 的应用列表中安装或导入此应用，再点击启动。应用由 Nimi 启动和管理；运行状态、App Access 和所需能力以 Nimi 的实际状态为准。

使用 AI 功能前，可在应用的「AI 配置」中设置所需能力，也可由 Nimi 打开同一份配置。缺少所需模型或能力时，相关功能会报告不可用，不会生成替代结果。

生成声音演示前，先从当前音频配置提供的声音列表中选择声音。生成结果仅是供你试听的本地候选。

创建角色时，可以生成参考图、使用现有候选，或导入 PNG、JPEG、WebP、GIF 图片（最大 8 MiB）。本地参考图会随草稿保留，重开时从 Runtime 恢复预览；选择它不会自动发布为 Realm 头像。若原素材已不可用，可清除选择或换一张图后继续。

此版本暂不支持导入音频文件。从素材库移除图片只移除该条本地引用；媒体文件本身由 Runtime 管理。

访问 Realm 数据需要 Nimi 中有效的账号与相应访问条件。

## 数据与支持

创建草稿、帖子草稿、本地排程、创作历史引用、本地素材和主题偏好通过 Nimi 提供的受保护存储访问。语言偏好保存在 Electron 的本机存储中。Realm 中的角色资料以 Realm 返回的记录为准。

帖子发布、定时发布和媒体上传尚未开放。本地排程仅记录计划时间，不发送提醒，也不会自动发布。

[使用与开发说明](https://github.com/nimiplatform/nimiapp-realm-persona-studio) · [反馈问题](https://github.com/nimiplatform/nimiapp-realm-persona-studio/issues)

# Design Audit Archive Instructions

This directory is a historical audit archive only.

- Do not use files in this directory as current product authority, current implementation briefs, or accepted UX scope.
- Current product authority lives in `.nimi/spec/project/kernel/**`.
- Current implementation source uses Realm Persona Studio naming, `RealmPersona`, and `WorldCore` / `RuntimeSourceSnapshot` surfaces.
- Historical references to Realm Agent Studio, RealmAgent, CharacterCard import, `listMyRealmAgents`, `getMyRealmAgent`, or agent-studio app ids are superseded and must not be revived as active requirements.
